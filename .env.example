# CockroachDB connection string from Cockroach Cloud
DATABASE_URL="postgresql://<username>:<password>@<host>:26257/<database>?sslmode=verify-full"

# JWT for auth cookie signing
JWT_SECRET="replace-with-a-strong-secret"

# Optional: AI integration key if you migrate exam generation endpoint
GOOGLE_GENERATIVE_AI_API_KEY=""
